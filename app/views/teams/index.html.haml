%h1.text-3xl.font-bold.text-gray-800.mb-4 Takımlar

%div.mb-4
  = link_to 'Yeni Takım', new_team_path, class: 'bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded transition duration-200'

- if @teams.any?
  %div.grid.grid-cols-1.sm:grid-cols-2.md:grid-cols-3.gap-6
    - @teams.each do |team|
      %div.bg-white.shadow-md.rounded-lg.p-6
        %h2.text-xl.font-semibold.text-gray-800.mb-2= team.name
        %p.text-gray-600.mb-4= team.description

        %div.flex.space-x-2
          = link_to 'Düzenle', edit_team_path(team), class: 'bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-2 px-4 rounded transition duration-200'
          = link_to 'Sil', team_path(team), method: :delete, data: { confirm: 'Takımı silmek istediğinize emin misiniz?' }, class: 'bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded transition duration-200'
- else
  %p.text-gray-600 Şu anda hiçbir takım bulunmamaktadır.
