%h1.text-3xl.font-bold.text-gray-800.mb-4 Yeni Takım Oluştur
%p.text-gray-600.mb-6 Takım bilgilerini giriniz.

= simple_form_for @team, html: { class: 'w-full max-w-lg' } do |f|
  - if @team.errors.any?
    .mb-4
      %h2.text-red-600.text-lg Takım oluşturma hataları:
      %ul.list-disc.list-inside
        - @team.errors.full_messages.each do |message|
          %li= message

  .mb-4
    = f.input :name, label: 'Takım Adı', input_html: { class: 'mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50' }

  .mb-4
    = f.input :description, label: 'Açıklama', as: :text, input_html: { class: 'mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50' }

  = f.submit 'Oluştur', class: 'mt-4 bg-green-500 hover:bg-green-600 text-white font-semibold py-2 px-4 rounded transition duration-200'

= link_to 'Geri', teams_path, class: 'mt-4 bg-gray-300 hover:bg-gray-400 text-gray-800 font-semibold py-2 px-4 rounded transition duration-200'
