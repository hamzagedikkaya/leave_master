%h1 Roller

%h2 Tüm Roller
%table
  %thead
    %tr
      %th ID
      %th Rol Adı
      %th Oluşturulma Tarihi
      %th İşlemler
  %tbody
    - @roles.each do |role|
      %tr
        %td= role.id
        %td= role.name
        %td= role.created_at.strftime("%Y-%m-%d %H:%M")
        %td= link_to 'Düzenle', edit_role_path(role), class: 'btn btn-primary'
= link_to 'Yeni Rol Oluştur', new_role_path, class: 'btn btn-success'
